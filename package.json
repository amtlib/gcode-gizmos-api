{
  "name": "keystone-app",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "dev": "keystone dev -p 3000",
    "start": "keystone prisma migrate deploy && keystone start",
    "build": "keystone build",
    "postinstall": "keystone postinstall",
    "migrate:deploy": "keystone prisma migrate deploy",
    "gen:migration": "keystone prisma migrate dev --create-only"
  },
  "dependencies": {
    "@keystone-6/auth": "^5.0.0",
    "@keystone-6/core": "^3.0.1",
    "@keystone-6/fields-document": "^5.0.0",
    "dotenv": "^16.1.4",
    "slugify": "^1.6.5",
    "typescript": "^4.8.0"
  }
}
